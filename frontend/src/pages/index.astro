---
import Layout from "../layouts/Layout.astro";
import HeroImage from '../components/HeroImage.astro';
import {lorem} from '../placeholder';
import { getLandingTitle } from "../api";
let landingTitle = await getLandingTitle();
---

<Layout title="Pure Intentions">
	<h1 class="text-5xl font-bold text-center">{landingTitle.header}</h1>
	<HeroImage />
      <section>
          <br />
          <p>{lorem.generateParagraphs(1)}</p>
          <br />
          <p>{lorem.generateParagraphs(1)}</p>
          <br />
          <p>{lorem.generateParagraphs(1)}</p>
      </section>
</Layout>

