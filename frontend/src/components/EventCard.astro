---
import BaseCard from "../layouts/BaseCard.astro";

interface Props {
  title: string;
  date: string;
  price: number;
  description: string;
}

let { title, date, price, description } = Astro.props;

const parsed = new Date(Date.parse(date));

const formatter = new Intl.DateTimeFormat("en-US", {
  timeZone: "America/New_York",
  timeStyle: "short",
  dateStyle: "short",
});
const formattedDate = formatter.format(parsed);
---

<BaseCard>
  <h2 class="text-xl font-black">{title}</h2>
  <h3 class="font-bold">{formattedDate}</h3>
  <p>{price}</p>
  <p>{description}</p>
</BaseCard>
