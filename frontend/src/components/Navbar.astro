---
import { getAllPages } from "../api";

let pages = await getAllPages();
let adminUrl = `${process.env.PAYLOAD_URL}/admin`;
---

<!-- <div id="navbar" class="group grid content-center bg-yellow-300/90 text-lg font-bold h-12 flex-none">
    <button>All Navlinks</button>
    <div class="font-sans text-center space-x-4 hidden group-hover:block">
        <a href="/" class="hover:text-green-700">Home</a>
        {pages.docs.map((page) => <a class="hover:text-green-700" href={`/${page.slug}`}>{page.title}</a>)}
        <a href="/contact" class="hover:text-green-700">Contact</a>
    </div>
    <a href={adminUrl} class="absolute right-6 hover:text-green-700">Login</a>
</div> -->
    
    <div class="group grid place-content-center bg-yellow-300/90 text-lg font-bold h-12">
        
        <button
        data-ripple-light="true"
        data-popover-target="menu"
        class="middle none center mr-3 py-3 px-6 font-sans text-m font-bold uppercase text-black hover:text-green-700 active:opacity-[0.85] disabled:pointer-events-none disabled:opacity-50 disabled:shadow-none"
        >
        Open Menu
        </button>
        <div class="font-sans text-center space-x-4 hidden group-hover:block">
            <ul
              role="menu"
              data-popover="menu"
              data-popover-placement="bottom"
              class="absolute z-10 min-w-[180px] overflow-auto rounded-md border border-blue-gray-50 bg-white p-3 font-sans text-sm font-normal text-blue-gray-500 shadow-lg shadow-blue-gray-500/10 focus:outline-none"
            >
    
              <a href="/" class="hover:text-green-700 block w-full cursor-pointer select-none rounded-md px-3 pt-[9px] pb-2 text-start leading-tight transition-all hover:bg-blue-gray-50 hover:bg-opacity-80 hover:text-blue-gray-900 focus:bg-blue-gray-50 focus:bg-opacity-80 focus:text-blue-gray-900 active:bg-blue-gray-50 active:bg-opacity-80 active:text-blue-gray-900">Home</a>

              {pages.docs.map((page) => <a class="hover:text-green-700 block w-full cursor-pointer select-none rounded-md px-3 pt-[9px] pb-2 text-start leading-tight transition-all hover:bg-blue-gray-50 hover:bg-opacity-80 hover:text-blue-gray-900 focus:bg-blue-gray-50 focus:bg-opacity-80 focus:text-blue-gray-900 active:bg-blue-gray-50 active:bg-opacity-80 active:text-blue-gray-900" href={`/${page.slug}`}>{page.title}</a>)}

              <a href="/contact" class="hover:text-green-700 block w-full cursor-pointer select-none rounded-md px-3 pt-[9px] pb-2 text-start leading-tight transition-all hover:bg-blue-gray-50 hover:bg-opacity-80 hover:text-blue-gray-900 focus:bg-blue-gray-50 focus:bg-opacity-80 focus:text-blue-gray-900 active:bg-blue-gray-50 active:bg-opacity-80 active:text-blue-gray-900">Contact</a>
            </ul>
        </div>
        <a href={adminUrl} class="absolute right-6 hover:text-green-700">Login</a>
    
        </div>
    </div>


